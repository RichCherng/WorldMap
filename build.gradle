plugins {
    id 'java'
    id 'application'
    id 'com.github.node-gradle.node' version '7.0.1'
    id 'com.google.protobuf' version '0.9.4'
}

group = 'com.worldmap'
version = '0.0.1-SNAPSHOT'
java.sourceCompatibility = JavaVersion.VERSION_21

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // Guice dependency injection
    implementation 'com.google.inject:guice:7.0.0'
    implementation 'com.google.inject.extensions:guice-servlet:7.0.0'
    implementation 'javax.inject:javax.inject:1'

    // Jetty web server
    implementation 'org.eclipse.jetty:jetty-server:11.0.18'
    implementation 'org.eclipse.jetty:jetty-servlet:11.0.18'
    implementation 'org.eclipse.jetty:jetty-webapp:11.0.18'

    // JAX-RS API and Jersey implementation
    implementation 'jakarta.ws.rs:jakarta.ws.rs-api:3.1.0'
    implementation 'org.glassfish.jersey.core:jersey-server:3.1.5'
    implementation 'org.glassfish.jersey.containers:jersey-container-servlet:3.1.5'
    implementation 'org.glassfish.jersey.inject:jersey-hk2:3.1.5'
    implementation 'org.glassfish.jersey.media:jersey-media-json-jackson:3.1.5'
    
    // Servlet API for web server integration
    implementation 'jakarta.servlet:jakarta.servlet-api:6.0.0'

    // HK2-Guice bridge for Jersey-Guice integration
    implementation 'org.glassfish.hk2:guice-bridge:3.0.5'
    implementation 'org.glassfish.hk2:hk2-api:3.0.5'
    implementation 'org.glassfish.hk2:hk2-locator:3.0.5'

    // JSON processing
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.16.1'
    implementation 'com.fasterxml.jackson.core:jackson-annotations:2.16.1'

    // Protocol Buffers
    implementation 'com.google.protobuf:protobuf-java:3.25.1'
    implementation 'com.google.protobuf:protobuf-java-util:3.25.1'

    // gRPC dependencies
    implementation 'io.grpc:grpc-netty-shaded:1.60.0'
    implementation 'io.grpc:grpc-protobuf:1.60.0'
    implementation 'io.grpc:grpc-stub:1.60.0'
    implementation 'io.grpc:grpc-services:1.60.0'

    // Armeria for gRPC-Web support
    implementation 'com.linecorp.armeria:armeria-grpc:1.32.0'

    compileOnly 'org.apache.tomcat:annotations-api:6.0.53' // For @Generated annotation

    // Configuration management
    implementation 'com.typesafe:config:1.4.3'

    // Firebase Admin SDK
    implementation 'com.google.firebase:firebase-admin:9.2.0'

    // Additional Google Cloud dependencies for Firestore
    implementation 'com.google.cloud:google-cloud-firestore:3.15.6'

    // Logging
    implementation 'org.slf4j:slf4j-simple:2.0.9'

    // Swagger/OpenAPI for Jersey
    implementation 'io.swagger.core.v3:swagger-jaxrs2-jakarta:2.2.20'
    implementation 'io.swagger.core.v3:swagger-integration:2.2.20'

    // Testing
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.1'
    testImplementation 'org.mockito:mockito-core:5.8.0'
    testImplementation 'org.mockito:mockito-inline:5.2.0'
    testImplementation 'org.mockito:mockito-junit-jupiter:5.8.0'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher:1.10.1'
}

// Node.js configuration for React build
node {
    version = '18.18.0'
    npmVersion = '9.8.1'
    download = true
    workDir = file("${project.projectDir}/frontend/.gradle/nodejs")
    npmWorkDir = file("${project.projectDir}/frontend/.gradle/npm")
    nodeProjectDir = file("${project.projectDir}/frontend")
}

// Task to build React app
task buildReact(type: com.github.gradle.node.npm.task.NpmTask) {
    description = "Build React application"
    workingDir = file("${project.projectDir}/frontend")
    args = ['run', 'build']
    dependsOn 'npmInstall'
    inputs.files(fileTree("frontend/src"))
    inputs.files(fileTree("frontend/public"))
    inputs.file("frontend/package.json")
    outputs.dir("frontend/build")
}

// Copy React build to static resources (for Jetty serving)
task copyReactBuild(type: Copy) {
    description = "Copy React build to static resources"
    from "frontend/build"
    into "src/main/resources/webapp"
    dependsOn buildReact
}

// Make sure React is built before processing resources
processResources {
    dependsOn copyReactBuild
}

// Application main class
application {
    mainClass = 'com.worldmap.WorldMapApplication'
}

tasks.named('test') {
    useJUnitPlatform()
    jvmArgs '--add-opens', 'java.base/java.lang=ALL-UNNAMED'
    jvmArgs '--add-opens', 'java.base/java.util=ALL-UNNAMED'
    jvmArgs '--add-opens', 'java.base/java.lang.reflect=ALL-UNNAMED'
    jvmArgs '--add-opens', 'java.base/sun.nio.ch=ALL-UNNAMED'
    systemProperty 'mockito.mock-maker.inline', 'true'
}

// Protobuf configuration
protobuf {
    protoc {
        artifact = 'com.google.protobuf:protoc:3.25.1'
    }
    plugins {
        grpc {
            artifact = 'io.grpc:protoc-gen-grpc-java:1.60.0'
        }
    }
    generateProtoTasks {
        all()*.plugins {
            grpc {}
        }
    }
}

// Add generated protobuf sources to source sets
sourceSets {
    main {
        proto {
            srcDir 'proto'
        }
        java {
            srcDirs 'build/generated/source/proto/main/java'
        }
    }
}